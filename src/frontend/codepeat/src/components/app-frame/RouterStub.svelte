<!-- Test stub standing in for svelte-spa-router's <Router> so ApplicationFrame can render
     its shell (navbar, footer, guards) without mounting the real route components. -->
<div data-testid="router-outlet"></div>
